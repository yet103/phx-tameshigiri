# docker compose が読む。コピーして .env にし、値を入れる（.env はコミットしない）。
# 運営端末（タブレット・スマホ）が採点画面・運営画面を開くときに入力する ID とパスワード。
# パスワードは十分長いランダム文字列にする。例: openssl rand -base64 24
# 記号（# $ 空白など）を含む場合は AUTH_PASS="..." のように引用符で囲む
AUTH_USER=
AUTH_PASS=
